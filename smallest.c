#include <stdio.h>

int main() {
    int a = 20, b = 300;

    if (a > b) {
        printf("a is the greatest number: %d\n", a);
    } else {
        printf("b is the greatest number: %d\n", b);
    }

    return 0;
}
