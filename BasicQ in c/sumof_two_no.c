#include <stdio.h> // headerfile 

int main() {  // main is function 
    int a = 20, b = 10, sum, sub , div , mul; 

    sum = a + b;
    sub = a - b;
    div = a / b;
    mul = a * b;

    printf("sum of two no %d\n", sum);
    printf("sub of two no = %d\n", sub);
    printf("div of a and b =%d\n",div);
    printf("mulipule a and b =%d\n",mul);
    printf("100%% sure");

    return 0;
}
/*
kuch aur common format specifiers

%d ------> int 
%f ------> float
%c ------> character
%s ------> string 
%lf -----> double

*/ 
